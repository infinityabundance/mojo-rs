//! Safe Rust message-pipe API (Phase 4: full system API above the core).
//! Scaffolded: the endpoint type is defined here; ownership semantics are
//! enforced by the C API layer during Phase 6.

/// A message-pipe endpoint (placeholder until the system API phase).
#[derive(Debug)]
pub struct MessagePipeEndpoint;
